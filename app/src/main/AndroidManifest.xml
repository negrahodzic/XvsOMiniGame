<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.myfirstexampleapp.negra.connect3game">

    <application
        android:allowBackup="true"
        android:icon="@drawable/ikonica"
        android:label="Superhero XvsO"
        android:roundIcon="@mipmap/ikonica_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".igraActivity"/>
        <activity android:name=".igra"/>
        <activity android:name=".MainActivity">
        <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>